version: "3.8"
services:
  # Web application
  roast_calculator:
    image: "roast_calculator"
    ports:
      - "6010:8000"
    networks:
      - roast_calculator
    restart: unless-stopped

networks:
  roast_calculator:
